#ifndef SIMULATION_H
#define SIMULATION_H

#include <Physics.hpp>

namespace CabanaPF {

template<std::size_t NumSpaceDim, std::size_t NumVariables>
class Simulation {
private:

public:
    Simulation(const CabanaPF::Physics &physics, const ) {

    }
};

}

#endif
